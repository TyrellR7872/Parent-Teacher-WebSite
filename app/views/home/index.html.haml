#main.container.home__grid
  -# TODO need to add images as carousel
  -# example: https://www.w3schools.com/bootstrap/bootstrap_carousel.asp

  .border.home__container__menu Menu placeholder // can be used to display news or notice
  .border.home__container__image This is where to add the images carousel of the school

  %h3.home__container__event--header Calendar Events

  -# .home__container__event
  -# .border.home__container__event__item--1 EVENT 1
  -# .border.home__container__event__item--2 EVENT 2
  -# .border.home__container__event__item--3 EVENT 3
  -# .border.home__container__event__item--4 EVENT 4

  .home__container__event
    -@calendar_events.each do |event|
      / Add a line to separate between events
      %span.home__container__event__item.border
        =link_to event.title, calendar_event_path(event)
        =event.start_date_time ? event.start_date: "No date indicated"
        =event.start_date_time ? event.start_time: "No time indicated"
        =event.location
        =truncate(event.description, length: 50)
        =event.contact_person

  / Room for footer?
  %br
  %br
