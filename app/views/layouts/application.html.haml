%html{ lang: 'en' }
  %head
    %meta{ charset: 'utf-8' }
    %title HCS-PTO
    = csrf_meta_tags
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'

  %body
    #header
      -# -if flash[:notice] || flash[:error] || flash[:success] || flash[:warning]
      .flash
        =render "flash_message"
      %nav.navbar
        =render "navbar"
    = yield

    %footer
